VITE_BACKEND_URL = "http://localhost:4000"

# Production: Sentinel SDK must call the deployed backend proxy directly.
# The Vite dev proxy handles /api/sentinel-proxy locally (relative path fallback).
# In Vercel (production), this MUST be set — the relative path won't resolve to Render.
VITE_SENTINEL_PROXY_URL=https://e-com-c0cw.onrender.com/api/sentinel-proxy